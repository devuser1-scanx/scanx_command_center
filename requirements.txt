alembic==1.16.2
fastapi==0.138.1
starlette==1.3.1
gunicorn==23.0.0
psycopg[binary]==3.2.9
pydantic-settings==2.10.1
python-dotenv==1.2.2
SQLAlchemy==2.0.41
uvicorn[standard]==0.35.0
PyJWT==2.13.0
pwdlib[argon2]==0.2.1
email-validator==2.2.0
python-multipart==0.0.31

# Not a direct dependency - pulled in transitively (observed inside the
# built Docker image, not in the local dev venv). Pinned explicitly to
# force the patched version and clear the Trivy HIGH finding for
# GHSA-6v7p-g79w-8964.
msgpack==1.2.1

# Explicitly require the first patched setuptools release so that the
# container image cannot retain the vulnerable version from its base layer.
setuptools>=78.1.1